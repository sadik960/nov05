version: 2
sources:
    - name: snowflake_data
      database: "{{('db1')}}"
      schema: raw_dev1
      tables:
        - name: raw_customer
        - name: raw_order
    - name: snapshot_tables
      database: "{{('db1')}}"
      schema: dbt_dev
      tables:
        - name: snap_customers_check

